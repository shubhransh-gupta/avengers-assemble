### Fix 2: Implement Sliding Window & TTL Eviction (DDoS Mitigation)
Never trust incoming traffic volume. Protect your memory footprint with strict bounds.

* **Action Item:**
  - Enforce Time-To-Live (TTL) expiration on all rate-limit keys so stale data auto-purges.
  - Implement an LRU (Least Recently Used) cache eviction policy or integrate a sliding-window counter using Redis sorted sets (`ZSET`).
  - Implement strict IP normalization and input sanitization before processing lookup keys to prevent parameter pollution.

### Fix 3: Defensive Coding & Input Sanitization
* **Action Item:**
  - Validate and sanitize all incoming headers (`X-Forwarded-For`, `Authorization`) against injection payloads before passing them to the rate-limiting keyspace.
  - Ensure zero fallback default secrets exist in production deployment builds. If environment variables are missing, the application must fail-secure and refuse to boot.

---

## AUDIT SIGN-OFF
The vulnerabilities have been isolated, tagged, and assigned counter-measures. Fix the ledger, secure the memory, and lock down the rate limiters. 

*I'm watching.*